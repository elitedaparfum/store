VITE_GOOGLE_CLIENT_ID=994433292881-5bcpaia5keifkb4059kpe2ae16g6cetj.apps.googleusercontent.com
# Backend is deployed as a Vercel serverless function at /api/* on this same domain
# (see vercel.json rewrites + api/index.js) — leave VITE_API_URL unset so requests
# are same-origin and never hit CORS. Do not point this at Railway; it's decommissioned.
